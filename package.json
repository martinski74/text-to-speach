{
  "name": "text-to-speech-vue",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.35.0",
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^6.1.0",
    "wrangler": "^4.87.0"
  }
}
